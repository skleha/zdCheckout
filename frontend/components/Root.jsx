import React from "react";
import { Provider } from "react-redux";
import { HashRouter } from "react-router-dom";
import App from './App';

const Root = () => (
  // Root takes in store
  // Provider
  // HashRouter
  <App />

);

export default Root;

